appId: com.example.unnamedproject
---
- launchApp
- assertVisible: "Open navigation menu" 

# We use a regex to ensure no encoded game_item tags exist.
- assertNotVisible:
    id: "game_item__index_.*"
